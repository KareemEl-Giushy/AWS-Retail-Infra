[defaults]
host_key_checking = False

[ssh_connection]
# Route all SSH traffic target connections through your bastion host
ssh_args = -o ProxyCommand="aws ssm start-session --target %h --document-name AWS-StartSSHSession --parameters 'port=%p'"