apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "retail-cart.fullname" . }}-test-connection"
  labels:
    {{- include "retail-cart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "retail-cart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
