apiVersion: v1
kind: Service
metadata:
  name: {{ include "retail-checkout.fullname" . }}
  labels:
    {{- include "retail-checkout.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "retail-checkout.selectorLabels" . | nindent 4 }}
