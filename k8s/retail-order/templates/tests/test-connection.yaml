apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "retail-order.fullname" . }}-test-connection"
  labels:
    {{- include "retail-order.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "retail-order.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
